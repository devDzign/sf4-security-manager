{% extends 'base.html.twig' %}

{% block title %}Modifie le meuble{% endblock %}

{% block body %}
    <h1>Modifie l'article</h1>
    <div class="row">


        <div class="col">
            {{ include('article/_form.html.twig', {'button_label': 'Update'}) }}
            {{ include('article/_delete_form.html.twig') }}
        </div>
    </div>

    <hr>
    <div class="row">

        <div class="col">
            <a href="{{ path('article_index') }}" class="btn btn-info btn-lg pull-left">
                <i class="fa fa-arrow-circle-left"></i> back to list
            </a>
        </div>
    </div>



{% endblock %}
